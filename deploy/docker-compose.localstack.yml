# LocalStack Community para os testes de deploy.
# Uso: docker compose -f deploy/docker-compose.localstack.yml up -d
# Tag 3.8.1 fixa: a partir da 2026.3.0 a :latest passou a exigir auth token.
services:
  localstack:
    container_name: localstack
    image: localstack/localstack:3.8.1
    ports:
      - "4566:4566"
    environment:
      - SERVICES=ecr,s3,cloudformation,lambda
      - DEBUG=1
      - DOCKER_HOST=unix:///var/run/docker.sock
    volumes:
      - "./.localstack-data:/var/lib/localstack"
      - "/var/run/docker.sock:/var/run/docker.sock"
